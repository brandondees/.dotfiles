# TODO: set up a script that'll run across macos and ubuntu,
# set up the transient dotfile alias,
# clone the repo bare,
# check out the repo,
# configure its own gitignore if needed,
# prompt for git user config values,
# and install and configure the preferred shell such as zsh + omz

